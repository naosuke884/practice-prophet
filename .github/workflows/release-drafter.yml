jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@v5
        with:
          config-path: .github/release-drafter.config.yml # 設定ファイルのパス
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
